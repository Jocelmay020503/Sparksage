[phases.setup]
nixPkgs = ["ffmpeg", "python311"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "echo 'FFmpeg location:' && which ffmpeg || find /nix/store -name ffmpeg -type f 2>/dev/null | head -1"
]

[start]
cmd = "python run.py"
